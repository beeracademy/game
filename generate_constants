#!/bin/sh
cd src
(
	echo "// Generated by generate_constants"
	hash=$(git rev-parse --short HEAD)
	echo "export const GIT_COMMIT_HASH = '$hash';"
	echo "export const ASSETS = ["
	find assets -type f -exec echo "  '{}'," ';'
	echo "];"
) > app/generated.ts
